// Single source of truth for the displayed version.
// Release convention: bump this AND package.json "version" together, then tag (vX.Y.Z)
// and publish a GitHub release — the in-app links point users there for updates.
export const VERSION = '1.7.0';
export const REPO_URL = 'https://github.com/howieyoung/ir-kit';
