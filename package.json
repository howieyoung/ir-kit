{
  "name": "ir-kit",
  "version": "1.5.0",
  "description": "The agent-native investor-relations kit: cap table + SAFE math, financial dashboard, investor CRM, update composer, IR playbooks, and an ir CLI with invariant checks built for coding agents. Zero dependencies; all data in local JSON.",
  "type": "module",
  "main": "server.js",
  "bin": { "ir": "bin/ir.js" },
  "scripts": {
    "start": "node server.js",
    "seed": "node scripts/seed-data.js",
    "init-workspace": "node scripts/init-workspace.js",
    "build-tutorial": "node scripts/build-tutorial.js"
  },
  "repository": { "type": "git", "url": "https://github.com/howieyoung/ir-kit.git" },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
